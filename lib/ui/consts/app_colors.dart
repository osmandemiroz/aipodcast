import 'dart:ui';

class AppColors {
  // Primary color palette
  static const Color primary = Color(0xFFFB5943);
  static const Color primaryLight = Color(0xFFFF7B67);
  static const Color primaryDark = Color(0xFFC23535);

  // Accent colors
  static const Color accent = Color(0xFFFFD001);
  static const Color accentLight = Color(0xFFFFE455);
  static const Color accentDark = Color(0xFFC29C00);

  // Neutral colors
  static const Color dark = Color(0xFF2A2A2A);
  static const Color darkLight = Color(0xFF3A3A3A);
  static const Color darkDark = Color(0xFF1A1A1A);

  // Placeholder and secondary colors
  static const Color placeholder = Color(0xFFC1BDCA);
  static const Color placeholderLight = Color(0xFFD6D3E0);
  static const Color placeholderDark = Color(0xFF9B98A3);

  // Dark mode placeholders
  static const Color darkPlaceholder = Color(0xFF373737);
  static const Color darkPlaceholderLight = Color(0xFF474747);
  static const Color darkPlaceholderDark = Color(0xFF272727);

  // Additional colors
  static const Color black = Color(0xFF3F3D56);
  static const Color white = Color(0xFFFFFFFF);
  static const Color transparent = Color(0x00000000);

  // Semantic colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);
}
